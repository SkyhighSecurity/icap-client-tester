A version of the script that works on python 2.7.5 (works on SWG Appliances < 13.0)


This version does not require the installation of the requests module but requires installation of pathlib (download here: https://pypi.org/project/pathlib/#files, extract and run "python setup.py install")


CLI ONLY, no GUI
